services:
  shibboleth-exporter:
    build: .
    ports:
      - "9090:9090"
    environment:
      - PORT=9090
      # METRICS_ENDPOINT is optional - for legacy /metrics endpoint
      # Use /probe?target=<url> for multi-target mode (recommended)
    restart: unless-stopped

